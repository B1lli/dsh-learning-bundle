# New installs use DSH native instruction files and native editing tools.
# Existing explicit storePath configurations retain legacy recall behavior.
- insert:
    - id: dsh-learning
      name: dsh-learning-bundle
      config:
        mode: native
